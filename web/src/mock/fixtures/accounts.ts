// The demo accounts the mock API seeds and the screenshot script signs in
// with: fixture data, never credentials of a real account.

export const DEMO_EMAIL = "camille@example.com";
export const DEMO_PASSWORD = "correct-horse-42";

/** The password the screenshot script gives the account it leaves unverified. */
export const UNVERIFIED_PASSWORD = "Unverified-pass-1";
